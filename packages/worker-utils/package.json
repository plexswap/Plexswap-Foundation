{
  "name": "@plexswap/worker-utils",
  "version": "3.1.3",
  "scripts": {
    "test": "vitest --run",
    "publish": "pnpm publish --access public"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  }
}
