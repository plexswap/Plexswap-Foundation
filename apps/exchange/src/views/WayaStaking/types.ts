export enum WayaLockStatus {
  NotLocked = 'NotLocked',
  Locking = 'Locking',
  Expired = 'Expired',
  Migrate = 'Migrate',
}

export enum WayaPoolType {
  NOT_MIGRATED = 0,
  MIGRATED = 1,
  DELEGATED = 2,
}
